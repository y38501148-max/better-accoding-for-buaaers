# 0.1.2 发布记录

2026-09-22：修复比赛选题入口与题序、无输入样例、比赛结束后的题库提交回退，并按用户要求将新建 C 模板改为 `int main()`。

- 源码：512acb5297c1a98b6e22c4a841b50634d0be9e37。
- VSIX：better-accoding-for-buaaers-0.1.2.vsix。
- SHA-256：106d6581169a1f6700d2790513ac896e1e537ab1158510191bd3cb8c86b3d7c2。
- GitHub Release：https://github.com/y38501148-max/better-accoding-for-buaaers/releases/tag/v0.1.2 。
- Marketplace：https://marketplace.visualstudio.com/items?itemName=muzermat.better-accoding-for-buaaers 。已上传同一份测试包，平台校验中。
- 本机 /Applications/VSC.app 已安装 0.1.2；安装的扩展代码、Webview 资源、清单与 VSIX 逐字节/字段比对一致。当前窗口需重新加载。

验证结果：

- 格式、lint、类型、构建、包白名单通过；24 项单元 + 88 项集成测试通过。
- CI 35708912583：Linux/Windows/macOS × 最低版本/Stable 六组全部通过。
- 独立用户/扩展目录安装 VSIX，激活、30:70 布局、未保存用例立即运行通过。
- 原生 VS Code：工作台选题按钮打开 A–J 顺序列表，筛选并确认 J 后，标题与右侧源码同时切到 J 对应路径。新建源码为正常空格缩进的 `int main()`。
- 只读核验真实 1309-A（全局题号 10470）：识别 1 个样例，输入 0 字节，预期输出 150 字节，无配对警告。未向该比赛或该题 POST；题面未写入测试 fixture。
- 模拟真实 HTTP 边界验证：已结束比赛仅向全局题号对应的题库路径发送一次；选择语言期间结束仍可回退；题库无权限、比赛权限错误、未知结束时间不误提交；网络结果未知不自动重试；跨窗口未确认记录仍受锁保护。

首次商店下载检查仍返回 0.1.1，已由版本检查明确拒绝当作 0.1.2 验收通过；等待平台校验完成后重试。本机日常配置安装的仍是已验证的 0.1.2，此检查使用独立目录，不影响本机安装。其余未覆盖的人工验收范围沿用 README。
