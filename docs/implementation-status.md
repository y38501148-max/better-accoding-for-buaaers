# 实施台账

方案：2026-09-22 v1.1。此文件记录实际实现与验证，不把目标写成已完成。

## 当前阶段

- 独立 TypeScript/VS Code 扩展工程已建立，版本 0.1.0 开发候选。
- 已实现双来源数据模型、HTTPS Cookie 客户端、账号密码登录与 SecretStorage 会话恢复、题库 HTML/比赛 JSON 导入、可选择比赛题目、题单树、左题右码工作台。
- 用例保存为独立 `.in/.out`，含 tests.json、originals、题面和题目元数据；事务 JSON 保留正文恢复快照。跨窗口写入锁、修订号和文件哈希保护保存；外部修改会导入为新修订。旧格式迁移先备份，写入中断通过日志恢复。
- 已实现卡片编辑、增删复制排序、启停、比较模式、保存 ACK、运行前 flush、同步保护、C/C++ 编译和 Judge、单例运行、重跑失败、编译诊断、输出限制、超时与取消。
- 已实现双来源提交适配器、精确语言、比赛提交前重排题序、私有提交日志、状态轮询；题库成功响应已实测为重定向到记录列表，适配器按新增 ID、本人、题号、语言和源码精确核对；扩展完整登录闭环仍需验收。
- macOS CodeLLDB 已通过真实 stdin / 断点 / 继续输出验收；Windows/Linux Debug 仍待验收。

## 验证证据

2026-09-22，macOS arm64：12 项单元测试 + 33 项集成测试通过。真实 Clang C/C++ 编译、EOF、输出洪泛、取消与后代进程清理通过。最低 VS Code 1.96.4 Extension Host 验证通过。

用户授权练习比赛 1306。真实浏览器学生端会话执行一次 C 提交：题号 10467，提交 ID 8287716，返回 WT，随后单条查询 AC，score=1。账号角色尚未确认为普通学生；未保存凭据、用户信息或真实题面 fixture。

## 发布阻断项（全部保留直到有证据）

- [ ] 账号密码登录 POST + 普通学生账号权限/失效实测。
- [x] 题库 #1 网页协议实测，提交 8287729 为 AC；源码内容核对的适配器 mock 回归通过。
- [ ] Webview 编辑后立即运行、关闭恢复、外部冲突和跨题迟到消息自动化/人工验收。
- [ ] 提交 UnknownOutcome 再次点击与重启恢复验收。
- [x] macOS CodeLLDB stdin、源码断点、变量与继续输出。
- [ ] Windows / Linux 实机工具链与 Debug（CI Judge 已通过）。
- [x] 最低 1.96.4、当前 Stable 1.138.0 Extension Host 与干净配置 VSIX 安装。
- [ ] 5–10 名学生、3–5 自然日内测（不可用模拟测试代替）。
- [ ] Marketplace publisher muzermat 注册与认证。
- [ ] 独立公开仓库创建、扫描、候选 VSIX、材料审核。
- [ ] 稳定版上架、全新商店安装及发布后观察。

## 已获授权

练习比赛 ID 1306；使用本机 gh 凭据创建与扩展同名的 GitHub 仓库；Marketplace publisher 使用 muzermat。题库测试题号 1 已获授权并完成一次 AC 提交。发布前完成方案质量门槛。

## 最新工程证据

- ego lite 使用真实打包 Webview JS/CSS 和合成宿主验证：两次编辑后立即运行都使用最新值；保存失败阻止运行并保留草稿；复制、排序、删除撤销后保存数量正确。
- 依赖更新后 npm audit 为 0；生产依赖许可证通知已生成。
- VSIX 开发候选打包通过，清单 77 项（含容器元数据），约 1.53 MB；校验和在 release/SHA256SUMS。
- 公开仓库：https://github.com/y38501148-max/better-accoding-for-buaaers ，私密漏洞报告已启用。
- Marketplace /manage 跳转 Microsoft 登录页，尚未建立发布会话或创建 publisher。

## 实现仍需补齐的具体项目

- 原生用例文件编辑与运行前保存已实现并通过宿主验收；复杂恢复事务遇到第三方改写时，目前保留现场报错，仍需提供更方便的恢复入口。
- 源码/编译配置/用例变化已标记结果过期；跨会话异常隔离仍需扩展验收。
- 缓存图片和离线图片；工作区级索引/比赛元数据仍需完善（当前索引为按 binding 分片的元数据）。
- 调试输入配置的路径转义与真实断点验收；Windows/Linux 的交互 Debug 验收（CI 进程清理已通过）。
- 本人历史记录分页、完整 VS Code 窗口重启验收与异常恢复交互。按持久化 ID 恢复查询和旧提交日志迁移已实现。
- 完整关闭/重开/窗口恢复草稿验收，发布文档中的完整双栏截图。

## UI 重做（2026-09-22）

按用户选择的简洁原生方向重做工作台：主题变量、统一 SVG 图标、紧凑题面、测试卡片及低频菜单。深色 640px 与模拟浅色主题 420px 面板视觉检查通过，无横向溢出。Webview 合成宿主重验编辑即运行使用最新值、保存失败阻止运行并保留草稿，菜单 Escape 正常关闭。候选 VSIX 干净配置安装、原生左右编辑组与保存 ACK 再次通过。

本轮验证使用合成题目，不替代真实 OJ 登录验收。截图保存在本机 release/previews。最低版 1.96.4 和当前 Stable 1.138.0 的本机宿主验证均已通过；CI Linux/macOS 两版本通过，Windows Unicode 路径编译仍在修复。

## 跨平台与调试进展

- CI 35694303637：Linux / Windows / macOS × VS Code 1.96.4 / stable 全部通过；包括中文与空格路径下的 C/C++ 编译、Judge 及原生宿主。MinGW 输出改用构建目录和相对文件名。
- macOS arm64，CodeLLDB 1.12.3，真实 Debug 验收通过：所选输入 41 被 scanf 读取，源码断点命中，变量 value=41，继续后输出 42，正常结束。使用官方 processCreateCommands 启动并显式指定输入文件，修复本机默认 launch 没有应用 stdin 重定向的问题。
- 用户指定默认左题右码比例为 3:7；VS Code 最左活动栏的 Accoding 入口提供题单、导入按钮和恢复布局按钮。

原生窗口验收发现 VS Code 的布局尺寸返回像素值，因此根据实际编辑区域宽度计算分栏，并在面板与代码显示后应用；避免焦点切换自动放大过窄组。默认 3:7，极窄窗口遵循 VS Code 的 220px 最小组宽。宿主测试新增实际宽度比例断言。220px 面板合成宿主检查无横向溢出。

## 文件模型与一致性验收（2026-09-22）

- 33 项集成测试包含：两个独立 Store 竞争写入仅一次成功、外部输入精确保留空白并阻断旧草稿、旧嵌入格式备份迁移、同修订号元数据外改、真实保存流程中注入中断并恢复、恢复期间第三方文本不被覆盖、实际退出进程遗留锁的恢复。
- 原生 Extension Host：打开实际 .in 文件，修改但不手动保存，立即 Judge 得到新输入对应输出 7。
- ego lite 合成宿主：外部修改时保持卡片草稿；选择已保存版本正确载入；明确用草稿覆盖后保存 ACK 正确；源码变化将 PASS 与汇总标为已过期。
- 编译语义回归：源码目录为 cwd，相对 -include 参数通过，同时以相对路径引用隔离的构建产物，继续验证 MinGW 的中文路径支持。
- 0fd72d8 CI 两个 Stable runner 的窗口可用宽度不足以满足严格 3:7，触发比例断言；测试现按 VS Code 220px 最小列宽明确检查窄窗口，不忽略布局检查。新提交将重跑六组 CI。

## 自动选择比赛导入权限（2026-09-22）

- 按用户补充要求，比赛导入与同步先确认 4000 管理端会话有效，再请求对应比赛题目 API，由服务端判定该账号的读取权限；不要求助教拥有比赛编辑权限；没有管理权限、登录失效或管理端不可达时自动回退学生端。界面无角色开关。
- 管理端连接仅允许 GET，共享当前账号会话的内存副本，隔离管理端 Set-Cookie；取消/退出登录会中止探测，不会重新发起学生端导入。每次导入和同步按比赛重新检查，无跨比赛权限缓存。
- 支持粘贴 4000 管理端比赛链接；题号、比赛 ID 与提交来源保持原有绑定。题库单题仍使用学生端；提交仍由原有学生端适配器处理。
- 真实协议读取验收：通过 ego lite 已登录会话作为 transport，运行实际构建的导入适配器，比赛 1306 先请求 `/api/users/me`，再请求 `/api/contests/1306`，解析 14 题、15 组题面样例，所有绑定比赛 ID 正确。未持久化真实题面、凭据或管理表单；该验收不替代扩展账号密码登录闭环。
- 12 项单元、46 项集成测试通过；新增用例覆盖只读权限、401/403、登录跳转、匿名会话、错误比赛 ID、端口不可达、限流、正文流中断、取消、跨比赛重检及管理端只读限制。
- 1f900d0 CI 的 Linux/macOS 通过，Windows 两组仅失败于新宿主断言把真实输出 CRLF 当作 LF。本次按 Judge 比较语义规范化该断言；Windows 实际编译、stdin 与输出数值均正确，等待新 CI。

- 809e7ce CI 五组通过；Windows 1.96.4 runner 的 C++ 集成测试超过测试框架默认 5 秒，提前清理目录导致 EBUSY。编译专用测试现允许 40 秒（覆盖插件 30 秒编译限制），运行用例的 500ms/100ms 超时断言保持不变。最新候选 VSIX 在干净配置安装后，原生 .in 编辑立即 Judge、分栏与活动栏命令均通过。

## 提交恢复与跨会话隔离（2026-09-22）

- 现按提交尝试持久化完整来源、语言、源码哈希、时间、发送状态与服务端 ID；账号哈希隔离，独立窗口写入互斥。原 append-only 日志只读迁移到 v2，原文件保留。
- 重新打开题目或恢复 Webview 时读取缓存记录，按已知 ID 查询所有 WT/JG；终态保存后停止，查询约两分钟后保留待评测状态，提供“恢复查询”。无 ID 的 Sending/UnknownOutcome 不自动重发，明确核对后才允许新尝试。
- 轮询更新合并已有历史，不再覆盖为单条记录；账号、题目切换、停止、清理缓存后的迟到响应受到检查，后台更新不切页、不替换测试输入框。
- 68 项测试（12 单元、56 集成）与最低版本 Extension Host 通过。新增 10 项集成测试包括重新实例化后的双来源/账号隔离、未决 ID 查询、未知结果显式核对、两个 Store 同时发送、旧日志迁移与保留、文件损坏不覆盖、取消后的迟到响应、最终状态不回退、查询预算耗尽后继续、并发刷新与 POST 回应合并。
- ego lite 合成工作台：在输入框编辑时发送后台提交消息，确认输入 DOM、文字和焦点保留；提交记录页显示待确认信息与恢复查询入口。
- 只读真实恢复验证：新建临时私有记录（8287716 标为 WT），重新实例化 Store，实际适配器只调用 `/api/users/me` 与 `/api/contests/1306/submission/8287716`，恢复为 Final/AC，账号一致。临时记录已删除，未发送新提交。这是存储与查询链路验证，完整 VS Code 进程重启验收仍保留。
- 上一提交 fe39a41 的 CI 35698558786 六组全部通过（Linux/Windows/macOS × 最低版本/stable）；本轮功能另行运行 CI。
