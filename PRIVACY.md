# 隐私说明

- 默认无遥测、无第三方分析服务。
- 账号密码仅在登录时发送到 Accoding 学生端 HTTPS；密码不持久化。Cookie 会话写入 VS Code SecretStorage。
- 导入和同步比赛时，使用当前会话的内存副本访问同一 Accoding 主机的 HTTPS 4000 管理端，按比赛检查读取权限；不可访问时回退学生端。导入连接不能交题，不保存完整响应页面或新增会话。
- 题面和公开样例保存在当前工作区；只缓存净化后的题面片段，不保存含账号/表单/CSRF 的完整页面。不要将私有课程题面放入公开仓库。
- 只有用户执行交题时，才把关联源码与必要题目/语言字段发往 Accoding；比赛路径不可用时，依次核验 4000 后端题库和学生端题库。后端写入限于当前全局题号的提交路径；状态查询允许官方只读 POST 接口，仅发送提交 ID。本地用例不上传。
- 提交 ID、来源、语言、时间、源码哈希和状态保存在扩展私有全局存储，按账号哈希隔离，不记录源码全文。
- 导入和同步时将题面图片缓存到工作区 `.better-accoding/images`；站内图片仅向固定学生端或 4000 管理端发送当前会话，站外 HTTPS 图片请求不携带 Cookie，原图片服务器能观察请求。打开已缓存题面时只读取本地图片。SVG 会净化，下载失败或格式不支持时保留提示。
- 导出诊断只包含系统、架构、扩展/VS Code 版本、信任状态与缓存题目数量，不包含账号、Cookie、源码和题面。
- “退出登录”清除会话；“清理个人提交缓存”清除私有提交日志。本地源码和题目缓存由用户自行备份、删除。
