# Changelog

## 0.1.7

- 修复 Markdown 提前消耗公式内反斜杠，导致 `x\%m` 被错误渲染为 `x` 的问题。
- 保留公式中的转义字符及矩阵换行，覆盖 `$…$`、`$$…$$`、`\(…\)`、`\[…\]`，仍对题面 HTML 净化。

## 0.1.6

- 识别 `输入样例 $1$` / `输出样例 $1$` 等用数学公式书写的样例序号，正确提取多组样例。
- 自动补回此前因解析失败而为空的样例缓存，保留源码、自建用例、已修改的样例和删除记录，无需重新导入。

## 0.1.5

- 切题复用右侧代码区并关闭其他已绑定题目的源码标签，保存未保存源码后再切换；用例文件不再产生额外代码栏。

- 本地评测默认忽略行末空格、制表符和输出末尾多余换行，兼容 CRLF/LF，保留行内内容和中间换行的比较。
- 已有用例自动迁移一次；仍可手动选择精确比较或 Token 比较，不修改源码及输入/预期输出文件内容。stderr 保留诊断，不参与答案比较。

## 0.1.4

- 修复 4000 后端提交详情格式差异导致的提交 ID 丢失；提交列表延迟出现时自动核对，避免重复发送代码。
- 自动轮询排队与评测状态，短暂查询失败会重试；提交中、排队、评测及错误显示红色，通过显示绿色。
- 重新打开题目时按账号、题号、语言、时间及源码指纹恢复之前未识别的题库提交，合并重复历史记录。

## 0.1.3

- 修复把赛前隐藏题单误判为“比赛不包含此题”的问题；交题依次核验比赛、4000 后端题库、学生端题库。
- 后端题库适配实际提交表单、源码传输编码和提交状态接口；记录保留端口来源，重启后仍能查询。
- 无权限时仅在实际交题请求发送前切换路径；发送后结果不确定或被拒绝均不自动重发。

## 0.1.2

- 工作台标题区增加常驻选题按钮；比赛题单、选择器和题面标题统一显示 A–J 等题序，并按比赛顺序排列。
- 支持“本题没有输入”和仅有输出样例的题目；运行时直接关闭标准输入，已缓存的缺失样例自动补齐。
- 比赛结束后自动使用对应全局题号走题库提交，核验题库权限与语言，并标注不计比赛成绩；结果未知时不会自动重发。
- C 代码模板改为 `int main()`，使用普通空格缩进，已有源码保持原样。

## 0.1.1

- 工作台确认完成题目和草稿恢复后再请求保存，避免页面慢加载时丢失保存确认。
- 登录入口直接显示在 Accoding 题单顶部、空题单欢迎页与工作台左下角，无需先导入题目或进入设置菜单。

## 0.1.0 — 首个公开版本

- 独立 VS Code 扩展、左题右码工作台与固定操作栏。
- 题库/比赛来源隔离、可选择导入、题面净化与本地公式资源。
- 题面图片本地缓存、离线阅读与 SVG 净化，站外下载不携带 OJ Cookie。
- 比赛导入与同步自动优先使用 4000 管理端读取权限，不可用时回退学生端，无需切换角色。
- 原生活动栏入口、默认 3:7 左题右码布局；独立用例文件、外部编辑冲突处理与结果过期提示。
- 带版本检查和原子写入的可编辑用例、官方样例同步保护。
- 比赛元数据、可重建工作区索引，新增题同步、移除题保护与恢复，保留首次选择导入范围。
- C/C++ 本地编译运行、比较模式、输出限制、取消、超时与编译诊断。
- 双来源提交协议适配、语言与题序核对、个人记录查询；比赛和题库网页协议均已完成真实 AC 验收，扩展账号密码登录完整流程仍待验收。
- 按账号持久化提交尝试、未决记录恢复查询、旧日志迁移与迟到响应隔离。
- macOS CodeLLDB 调试及三平台 CI 编译/评测/扩展宿主验证；Windows/Linux 交互调试仍待验证。
